%header sucks.Berkeley
%section
  %div{ :id => "center-container" }
    %div{ :id => "tracking-box" }
      -# 
        @issue_name
      Airbears Sucks!
    %div{ :id => "main-box" }
      = form_tag do
        = label :issue_action, "What were you doing when the problem occurred?", ''
        %br
        %br
        = text_area :put, :issue, :id => 'what-action-field'        
        = submit_tag '', :id => 'what_sucks_submit'

%section
  %div{ :id => "center-container" }
    %div{ :id => "main-box" }
      = form_tag do
        = text_field :put, :issue_name, :name => 'what-text'
        = label :issue, :name, "sucks!"
        = submit_tag '', :id => 'submit_arrow'
